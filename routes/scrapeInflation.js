const express = require('express');
const router = express.Router();
const EmasPerak = require('../models/emasperak');

/* scrapes inflation data and save to mongodb. */
router.get('/', async (req, res) => {
});

module.exports = router;
